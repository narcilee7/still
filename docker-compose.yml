services:
  postgres:
    image: postgres:16-alpine
    container_name: still-postgres
    environment:
      POSTGRES_USER: still
      POSTGRES_PASSWORD: still
      POSTGRES_DB: still
    ports:
      - "5432:5432"
    volumes:
      - still_postgres_data:/var/lib/postgresql/data

volumes:
  still_postgres_data:
